# Dev profile - local Docker Postgres, verbose logging
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/telcoedge?sslmode=disable
    username: telcoedge
    password: telcoedge_dev

  jpa:
    show-sql: true


logging:
  level:
    org.hibernate.sql: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE